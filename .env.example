# Rails Pulse Development Environment Variables
# Copy this file to .env and customize the values for your local setup

# PostgreSQL Configuration
POSTGRES_USERNAME=postgres
POSTGRES_PASSWORD=
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# MySQL Configuration
MYSQL_USERNAME=root
MYSQL_PASSWORD=
MYSQL_HOST=localhost
MYSQL_PORT=3306

# Test Database Configuration
# Uncomment to override default database adapter for testing
# DB=postgresql
# DB=mysql2
# FORCE_DB_CONFIG=true

# MySQL specific test settings
# PARALLEL_WORKERS=1

# Optional: Use in-memory database for faster unit tests
# MEMORY_DATABASE=true
